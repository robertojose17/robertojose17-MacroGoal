{
  "build": {
    "development": {
      "developmentClient": true,
      "distribution": "internal",
      "autoIncrement": true,
      "prebuildCommand": "node scripts/patch-folly.js && npx expo prebuild --non-interactive && node scripts/patch-podfile.js"
    },
    "preview": {
      "distribution": "internal",
      "autoIncrement": true,
      "prebuildCommand": "node scripts/patch-folly.js && npx expo prebuild --non-interactive && node scripts/patch-podfile.js"
    },
    "production": {
      "autoIncrement": true,
      "prebuildCommand": "node scripts/patch-folly.js && npx expo prebuild --non-interactive && node scripts/patch-podfile.js"
    }
  }
}
